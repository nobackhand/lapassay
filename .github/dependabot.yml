version: 2
updates:
  - package-ecosystem: nuget
    directory: "/"
    schedule:
      interval: weekly
    groups:
      # Vortice packages version together; one PR instead of three.
      vortice:
        patterns: ["Vortice.*"]
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
